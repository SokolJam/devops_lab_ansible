{% for host in groups['appservers'] %}
<VirtualHost *:80>
  ProxyPass        / http://{{ hostvars[host].ansible_host }}:8080/
  ProxyPassReverse / http://{{ hostvars[host].ansible_host }}:8080/
  ProxyRequests     Off
    
</VirtualHost>
{%endfor%}

